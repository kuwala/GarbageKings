using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimerMan : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if enough time passed 
        // trigger a you loose / reset? 
        // maybe something

        // two options to do this
        // make a timer enum thinggy
        // count frames

        // but whats the point. what would I do when
        // THe timer runs out?
        // a game over screen? maybe that should be 
        // the timer for bringing back enough "food" Garbage
        // 
        // Oh I love this mix. Episode 49 - Julien Mier
        //
        // nnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn
        
    }
}
